/** @type {import('@vue/cli-service').ProjectOptions} */
const config = {
  productionSourceMap: false,
  css: {
    extract: false
  },
  pwa: {
    name: 'Note',
    themeColor: '#4A4A4A'
  }
}

module.exports = config
